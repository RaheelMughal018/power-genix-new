[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build:backend && find /app -name 'main.js' 2>/dev/null"

[deploy]
startCommand = "node backend/dist/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3